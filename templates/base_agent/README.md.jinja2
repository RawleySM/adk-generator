# {{ project_name }}

{{ description }}

## Installation

```bash
pip install -r requirements.txt
```

## Usage

### Running the Agent

```python
from google.adk.runners import Runner
from google.adk.sessions import InMemorySessionService
from {{ project_name }} import app

# Create a runner
runner = Runner(
    agent=app.root_agent,
    app_name="{{ project_name }}",
    session_service=InMemorySessionService()
)

# Run the agent
result = runner.run("Your query here")
print(result)
```

### Using the CLI (if implemented)

```bash
python -m {{ project_name }}.cli
```

## Project Structure

```
{{ project_name }}/
├── agent.py          # Main agent definition
├── app.py            # ADK App wrapper
├── __init__.py       # Package initialization
{% if has_tools %}├── tools.py          # Tool definitions{% endif %}
{% if has_callbacks %}├── callbacks.py      # Callback implementations{% endif %}
├── requirements.txt  # Python dependencies
└── README.md         # This file
```

## Features

- **Model**: {{ model }}
{% if has_tools %}- **Custom Tools**: Yes{% endif %}
{% if has_callbacks %}- **Callbacks**: Yes (logging, validation, etc.){% endif %}
{% if has_memory %}- **Session Management**: Yes{% endif %}

## Development

### Running Tests

```bash
pytest tests/
```

### Code Quality

```bash
# Format code
black {{ project_name }}/

# Type checking
mypy {{ project_name }}/

# Linting
pylint {{ project_name }}/
```

## License

Apache 2.0

## Generated by

ADK Generator - https://github.com/RawleySM/adk-builder-manus
